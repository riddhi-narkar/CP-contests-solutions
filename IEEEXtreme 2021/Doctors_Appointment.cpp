#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    while(t--)
    {
        int n, l, r;
        cin >> n;
        vector <pair <int, int> > vec;

        for(int i = 0; i<n; i++)
        {
            cin >> l >> r;
            vec.push_back(make_pair(l, r));
        }


    }
    return 0;
}